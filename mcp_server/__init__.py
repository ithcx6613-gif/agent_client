"""
MCP Server — tools for Azure AI Foundry Agent.

This module implements a FastMCP server that exposes tools the AI Foundry
agent can call on behalf of the signed-in user, using OAuth 2.0 On-Behalf-Of
(OBO) flow for downstream API calls.
"""

__version__ = "0.1.0"
